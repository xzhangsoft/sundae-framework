import { CanActivateViaAuthGuardDirective } from './can-activate-via-auth-guard.directive';

describe('CanActivateViaAuthGuardDirective', () => {
  it('should create an instance', () => {
    const directive = new CanActivateViaAuthGuardDirective();
    expect(directive).toBeTruthy();
  });
});
